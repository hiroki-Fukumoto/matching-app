name: matching_app
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.17.1 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  openapi:
    path: ./openapi/client

  hooks_riverpod:
  state_notifier:
  freezed_annotation:

  cupertino_icons: ^1.0.2
  flutter_dotenv: ^5.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0
  provider: ^5.0.0
  flutter_riverpod: ^1.0.0
  build_runner:
  freezed:
  font_awesome_flutter: ^10.1.0-nullsafety

flutter:
  uses-material-design: true
  assets:
    - assets/img/
    - .env
